<!DOCTYPE html>
<html>

    <head></head>

    <body>

        <h1>Recipes: </h1>
        <% @recipes.each do |r| %>
        <%= r.name %>
        <%= r.ingredients %>
        <%= r.cook_time %>
        <a href=<%="/recipes/#{r.id}"%>>more...</a><br>
        <% end %>
        
    </body>

</html>